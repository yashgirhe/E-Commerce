spring.application.name: config-server

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yashgirhe/E-Commerce-config-server
#          username: yashgirhe
#          password:
          default-label: main
server:
  port: 8888
